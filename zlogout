# ----------------------------------------------------------------------
# ~/.dotfiles/zlogout
# Mark Spain
# ----------------------------------------------------------------------

# source the bash_logout script so this works in both zsh and bash
# --------------------------------------
[[ -a $HOME/.bash_logout  ]] && . $HOME/.bash_logout
